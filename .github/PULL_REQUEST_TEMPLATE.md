## Summary
<!-- What does this PR change and why? -->

## Changes
-

## Checklist
- [ ] `npm run typecheck` passes (tsc --noEmit)
- [ ] `npm test` passes (vitest)
- [ ] `npm run build` succeeds (tsup → dist/)
- [ ] `npm run smoke` passes (8 tools / 2 resources / 1 prompt over stdio)
- [ ] `npm run inspector` conformance still green (if tool/resource/prompt counts changed, smoke + Inspector updated)
- [ ] Tests added/updated for the change
- [ ] Honesty preserved: `wc_edge` degrades (never fabricates an edge/receipt), spend cap never self-raised, no placeholder tx presented as real
- [ ] Docs / README updated if needed

## Related Issues
Closes #
